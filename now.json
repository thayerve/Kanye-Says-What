{
    "version": 2,
    "name": "with-api",
    "builds": [{ "src": "with-api/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "with-api/$1" }
    ]
}
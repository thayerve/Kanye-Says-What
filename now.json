{
    "version": 2,
    "name": "kanye-quote",
    "builds": [{ "src": "with_api/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "with_api/$1" }
    ]
}